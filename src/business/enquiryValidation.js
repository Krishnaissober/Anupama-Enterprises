export const limits={name:100,company:150,email:254,phone:40,requirement:1500,message:2000};
export function validateEnquiry(values){const errors={};if(!values.name.trim())errors.name='Enter your name.';if(!values.email.trim())errors.email='Enter your email.';else if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(values.email.trim()))errors.email='Enter a valid email address.';if(values.phone.trim()&&(!/^[+\d\s().-]+$/.test(values.phone)||values.phone.replace(/\D/g,'').length<7||values.phone.replace(/\D/g,'').length>15))errors.phone='Use 7–15 digits; spaces, brackets and a leading + are allowed.';if(!values.requirement.trim())errors.requirement='Describe your requirement.';for(const [key,max] of Object.entries(limits))if(values[key].length>max)errors[key]=`Keep this field to ${max} characters or fewer.`;return errors;}
